  <% provide(:title, "Edit groupe") %>
  <h1>Modifier le groupe</h1>
  <%= render partial: "form", locals: {groupe: @groupe} %>
  
  <h3>Supprimer le groupe</h3>

  <p>Unhappy? <%= button_to I18n.t('button.destroy'), groupe_path(@groupe), data: { confirm: I18n.t('button.are_you_sure') }, method: :delete %></p>

  <%= link_to I18n.t('button.back'), :back %>
